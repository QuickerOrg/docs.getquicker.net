# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

本仓库已有 `AGENTS.md`，其中包含完整的工作规则（沟通、文档编写、Git、验证等）。本文件只补充架构层面的“全局认知”，两者一并遵守；`AGENTS.md` 与本文件冲突时以 `AGENTS.md` 中更具体的约束为准。

## 项目概览

Quicker 文档站，基于 **Docusaurus 3.10 + TypeScript + npm**。默认语言为简体中文（`zh-Hans`），新增内容也默认用中文。Node 要求 `>=20`。

## 常用命令（PowerShell）

```powershell
npm install                                              # 安装依赖
npm run start                                            # 本地预览，默认 http://localhost:3000/
npm run start -- --host 127.0.0.1 --port 3000 --no-open  # AGENTS.md 推荐的预览方式
npm run typecheck                                        # tsc 类型检查（改 src/ 或 *.ts 后必跑）
npm run build                                            # 生产构建，产物在 build/
npm run clear                                            # 清理 .docusaurus 缓存，构建异常时先试
```

验证策略（见 `AGENTS.md`）：改 `docs/` 的路径/链接/front matter/MDX/`sidebars.ts` 后跑 `npm run build`；改 `src/`/`docusaurus.config.ts`/依赖后先 `npm run typecheck` 再 `npm run build`。本站没有测试套件，构建即主要验证手段。`onBrokenLinks: 'throw'`，因此死链会直接让 `build` 失败。

## 架构要点（需跨文件理解的部分）

**文档在站点根路径提供。** `docusaurus.config.ts` 设置 `routeBasePath: '/'`，所以 `docs/v2/getting-started.md` 对应 URL `/v2/getting-started`，没有 `/docs` 前缀。不要为兼容编辑器/工具改回 `/docs`（这也是 Editsaurus 0.5.x 适配不佳的原因）。

**侧边栏由文件系统自动生成。** `sidebars.ts` 用 `{type: 'autogenerated', dirName: '.'}`，新增文档放进 `docs/` 对应目录即可自动出现，无需手改 `sidebars.ts`。排序与分类由两处控制：文档 front matter 的 `sidebar_position`，以及各目录下的 `_category_.json`（`label`/`position`/`link`）。一个分类要有 landing 页时，用 `_category_.json` 的 `link`（`type: doc` 指向某文档，或 `type: generated-index` 自动生成索引页）。

**评论系统是一条跨文件的数据流**，接入真实后端前请把它作为整体理解：
1. 文档 front matter 声明 `quickerDocKey`（稳定的评论映射键，尽量不随文件路径变化）和 `comments`（是否启用）。
2. `src/theme/DocItem/Footer/index.tsx` 是对 Docusaurus 默认 `DocItem/Footer` 的 swizzle 覆盖，读取上述 front matter；`comments !== false` 时启用，`quickerDocKey` 缺省回退到 `metadata.id`。
3. 它渲染 `src/components/QuickerComments`，该组件把 `docKey`/`title` 拼到 `siteConfig.customFields.quickerCommentsBaseUrl` 的查询参数后用 iframe 挂载。
4. `quickerCommentsBaseUrl` 当前为空字符串（见 `docusaurus.config.ts`），所以评论区暂不渲染——这是占位实现，**后端接口尚不存在**，接入前需先确认接口、鉴权、跨域、垃圾评论与数据映射方案。

## 目录约定

- `docs/`：面向最终用户的 Markdown/MDX 正文，按产品功能与用户任务组织。每个子目录的 `_category_.json` 控制该分类的标题与排序。
- `src/`：站点代码、主题覆盖（`src/theme/`）、组件（`src/components/`）、全局样式（`src/css/custom.css`）。不放普通文档正文。
- 图片优先**就近存放**：放在文档同级 `img/` 目录，相对路径 `./img/xxx.png` 引用（构建时校验、与文档一起增删）。多篇共用图或全局示意图才放 `static/img/`（绝对路径 `/img/...`）。
- `static/`：原样复制到站点根的静态资源；全局共用图放 `static/img/`，附件/下载放 `static/files/` 或 `static/downloads/`。
- `.docusaurus/`、`build/`、`node_modules/`：生成或依赖目录，不提交。

## 新增文档 front matter 约定

```md
---
title: 页面标题
description: 搜索与分享摘要
quickerDocKey: v2/install/windows   # 稳定的评论映射键，尽量不随路径变化
comments: true                      # 是否启用评论
sidebar_position: 1                 # 可选，分类内排序
---
```

# Screenshot → component review state
#
# Written by `npm run docs:review:api` when reviewers check “已审核”.
# Safe to commit so the team shares fold progress.
#
# Catalog (regenerated, do not hand-edit): catalog.json
#   source=orphan  leftover img + preview
#   source=direct  MDX-authored preview (no img), e.g. StepProgramView

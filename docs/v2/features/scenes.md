---
title: 场景与分组
description: 在新面板里按「场景 → 分组 → 动作」组织入口，以及场景如何和触发规则共用。
sidebar_position: 30
quickerDocKey: v2/features/scenes
comments: true
---

# 场景与分组

V2 的面板按 **场景 → 分组 → 动作** 组织。场景回答「当前在哪个上下文」，分组用来替代 1.x 里「同一程序下的多张动作页」。

## 日常怎么用

- **全局区**：不依赖当前程序、希望随时能用的动作。
- **上下文区**：随前台程序或网址变化；末尾的 **通用** 适合所有程序都要用的动作。
- 单击场景标签切换；双击打开「场景与动作管理」；右键可新建、导入或从旧动作页引入动作。
- 开启「显示分组」后，组在同一列表里连续显示，右侧导航可跳转。
- 动作本体和入口是分开的：从场景移除只去掉当前位置，删除才会去掉本体。

完整操作（启用新面板、置顶、拖动、批量移动、锁定上下文）见 [新面板窗口的使用](/v2/what's-new/new-main-win/usage.md)。

## 场景和触发是同一套上下文

快捷键、轮盘、手势、文本指令等规则也按场景生效。更具体的场景可以覆盖或禁用上级规则。统一编辑入口在 **场景与动作管理**。

相对 1.x 的管理方式和诊断见 [场景与触发方式](/v2/what's-new/scenes-and-triggers.md)。各入口怎么选见 [快速触发概览](./triggers/index.md)。

需要用动作切到某个场景、分组或旧动作页时，见 [加载场景与加载动作页](/v2/migration/new-main-win/load-action-page.md)。
